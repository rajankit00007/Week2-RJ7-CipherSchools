// const EMAIL_REG_EX =
// export const isEmail = (email) => {
//     if(typeof email !== "string") {
//         return falsel;
//     }
//     return EMAIL_REG_EX.test(email);

// };
// export const isValidUser = (user) => 
//    isEmail(user.email) && user.password.length && user.firstName && user.lastName;
        

